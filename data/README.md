# Data

No benchmark is selected or downloaded yet. Record source, license, version, preprocessing, split IDs/hashes, and duplicate handling before use. Keep train/development, search, and final audit questions separate as specified in the research protocol. Raw data and model outputs belong in ignored `raw/` and `cache/` directories or agreed external storage.

A precomputed response matrix is an evaluator-owned reference. The search policy can request a cell but must not inspect other cells, oracle scores, or hidden labels. Partial/failed executions must remain distinguishable from unqueried cells.
