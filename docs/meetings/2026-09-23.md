# Meeting material supplied on September 23, 2026

## Provenance and limits

Read from two screenshots named `Screenshot_2026-09-23_21-02-35.png` and `Screenshot_2026-09-23_21-02-38.png`, and the whiteboard photograph `微信图片_20260923210451_400_36.jpg`, originally in `/home/gabi/agentic_6113`. Original files remain unchanged and local. The user identifies the whiteboard author as the postdoc mentor.

The screenshots show **COMS6113 Group 18 Literature Review**. Only the visible content was read; the full live document, hidden comments, and the Problem statement / Sept 22 / Experiment tabs were not retrieved. The attempted document export did not succeed. This is an interpretation of supplied material, not approved meeting minutes.

## Shared document: visible content

The stated problem is to choose models efficiently from the client side for a multi-stage pipeline, balancing performance and cost and improving on random search. One question asks when retries help and when systems should retry.

The central reading list contains AgentOpt, VineLM, BanditEval, Efficient Cost-Aware LLM Evaluation, and SySRs. It also names SCOPE, PromptEval, and RouterBench. A separate list concerns coding-agent harness design, including Beyond Prompts. The second screenshot discusses optimizing a fixed model's surrounding prompts and tool-boundary behavior. These are contextual readings, not evidence that this project has selected harness optimization as its scope.

## Whiteboard: legible concepts

- A response matrix with model/configuration rows and question columns; individual entries appear to be binary outcomes.
- A move from one objective, such as accuracy, to multiple objectives including accuracy, cost, and latency.
- Gittins / efficient cost-aware evaluation, SySRs, BanditEval with UCB and low-rank ideas, PromptEval, AgentOpt, VineLM, and SCOPE.
- A decreasing curve with **search cost** on the horizontal axis and **simple regret** on the vertical axis; annotations describe oracle accuracy minus recommended accuracy.
- Separate deployment/inference cost discussion, input/output token counts, known versus unknown costs, and benchmark examples including MMLU.
- A multi-stage sketch with P, S, V and model assignments. The exact role names and some handwritten labels/numbers are unclear; they are not treated as agreed specifications.

## User's interpretation and commitments

The user will establish the group repository. The aim is eventual publishable work, with teammates, Codex, and Claude Code collaborating. Configuration search is closely related to hyperparameter search. A configuration is an arm; testing it on a benchmark question is an observation. The user reports that the mentor approved investigating how known structural similarities between configurations can help search.

Working assumptions still need validation: response repeatability, the usable retry checker, benchmark choice, exact model costs, and whether structural similarity predicts outcome similarity. The paper reading notes check the cost and sampling claims independently.
